type Item = 
 record {
   addedBy: principal;
   id: nat;
   name: text;
   quantity: nat;
 };
service : {
  addItem: (name: text, quantity: nat) -> (text);
  editItem: (id: nat, newName: text, newQuantity: nat) -> (text);
  listItems: () -> (vec Item) query;
}
